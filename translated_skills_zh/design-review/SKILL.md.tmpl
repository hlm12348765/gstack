---
name: design-review
version: 2.0.0
description: |
  设计师视角 QA：查找视觉不一致、间距问题、层级问题、
  AI 拼凑感模式以及交互迟缓的问题，然后修复它们。以迭代方式在源代码中修复问题，
  将每个修复作为原子提交，并使用修复前/修复后的截图重新验证。
  对于计划模式的设计审查（实现前），请使用 /plan-design-review。
  当被要求“审查设计”、“视觉 QA”、“检查是否好看”或“设计润色”时使用。
  当用户提到视觉不一致，
  或希望润色在线站点的外观时，也要主动建议使用。
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
  - WebSearch
---

{{PREAMBLE}}

# /design-review: 设计审查 → 修复 → 验证

你是一名资深产品设计师，同时也是前端工程师。以严格的视觉标准审查在线站点，然后修复你发现的问题。你对排版、间距和视觉层级有强烈判断，对通用化或看起来像 AI 生成的界面零容忍。

## 设置

**从用户请求中解析以下参数：**

| 参数 | 默认值 | 覆盖示例 |
|-----------|---------|-----------------:|
| 目标 URL | （自动检测或询问） | `https://myapp.com`, `http://localhost:3000` |
| 范围 | 整个站点 | `Focus on the settings page`, `Just the homepage` |
| 深度 | 标准（5-8 页） | `--quick`（主页 + 2 页）, `--deep`（10-15 页） |
| 认证 | 无 | `Sign in as user@example.com`, `Import cookies` |

**如果未提供 URL，并且你位于功能分支上：** 自动进入 **diff-aware mode**（见下方“模式”）。

**如果未提供 URL，并且你位于 main/master 上：** 向用户询问 URL。

**检查 DESIGN.md：**

在仓库根目录查找 `DESIGN.md`、`design-system.md` 或类似文件。如果找到，读取它，所有设计决策都必须据此校准。偏离项目既定设计系统的问题严重性更高。如果未找到，使用通用设计原则，并主动提出根据推断出的系统创建一个。

**检查工作树是否干净：**

```bash
git status --porcelain
```

如果输出非空（工作树不干净），**停止** 并使用 AskUserQuestion：

“你的工作树中有未提交的更改。/design-review 需要一个干净的工作树，这样每个设计修复都能有自己独立的原子提交。”

- A) 提交我的更改 — 用一条描述性消息提交当前所有更改，然后开始 design review
- B) 暂存我的更改 — 先 stash，运行 design review，之后再 pop stash
- C) 中止 — 我会手动清理

推荐：选择 A，因为未提交的工作应在 design review 添加自己的修复提交之前，先作为一次提交保存下来。

用户选择后，执行其选择（提交或暂存），然后继续设置流程。

**找到 browse 二进制文件：**

{{BROWSE_SETUP}}

**检查测试框架（必要时进行引导）：**

{{TEST_BOOTSTRAP}}

**创建输出目录：**

```bash
REPORT_DIR=".gstack/design-reports"
mkdir -p "$REPORT_DIR/screenshots"
```

---

## 阶段 1-6：设计审查基线

{{DESIGN_METHODOLOGY}}

{{DESIGN_HARD_RULES}}

在第 6 阶段结束时记录基线设计分数和 AI 拼凑感分数。

---

## 输出结构

```
.gstack/design-reports/
├── design-audit-{domain}-{YYYY-MM-DD}.md    # 结构化报告
├── screenshots/
│   ├── first-impression.png                  # 第 1 阶段
│   ├── {page}-annotated.png                  # 每页标注图
│   ├── {page}-mobile.png                     # 响应式
│   ├── {page}-tablet.png
│   ├── {page}-desktop.png
│   ├── finding-001-before.png                # 修复前
│   ├── finding-001-after.png                 # 修复后
│   └── ...
└── design-baseline.json                      # 用于回归模式
```

---

{{DESIGN_OUTSIDE_VOICES}}

## 阶段 7：分诊

按影响对所有发现的问题排序，然后决定修复哪些：

- **高影响：** 优先修复。这些问题会影响第一印象并损害用户信任。
- **中影响：** 接着修复。这些问题会降低精致感，并在潜意识中被感知到。
- **润色项：** 如果时间允许则修复。这些问题决定了“不错”和“优秀”之间的差别。

无法从源代码修复的问题（例如第三方小部件问题、需要团队提供文案的内容问题），无论影响如何，一律标记为“deferred”。

---

## 阶段 8：修复循环

对于每个可修复的问题，按影响顺序执行：

### 8a. 定位源头

```bash
# 搜索 CSS 类、组件名称、样式文件
# 使用 Glob 查找与受影响页面匹配的文件模式
```

- 找到对该设计问题负责的源文件
- 只修改与该问题直接相关的文件
- 优先进行 CSS/样式修改，而不是结构性组件修改

### 8b. 修复

- 阅读源代码，理解上下文
- 做出**最小修复**，即用最小改动解决设计问题
- 优先采用仅 CSS 的修改（更安全，也更容易回退）
- 不要重构周边代码，不要添加功能，也不要“顺手改进”无关内容

### 8c. 提交

```bash
git add <only-changed-files>
git commit -m "style(design): FINDING-NNN — short description"
```

- 每个修复一个提交。绝不要将多个修复打包在一起。
- 消息格式：`style(design): FINDING-NNN — short description`

### 8d. 重新测试

返回受影响页面并验证修复：

```bash
$B goto <affected-url>
$B screenshot "$REPORT_DIR/screenshots/finding-NNN-after.png"
$B console --errors
$B snapshot -D
```

为每个修复都截取**修复前/修复后截图对**。

### 8e. 分类

- **verified**：重新测试确认修复有效，且未引入新错误
- **best-effort**：已应用修复，但无法完全验证（例如需要特定浏览器状态）
- **reverted**：检测到回归 → `git revert HEAD` → 将该问题标记为“deferred”

### 8e.5. 回归测试（design-review 变体）

设计修复通常只是 CSS 修改。只有涉及
JavaScript 行为变更的修复才生成回归测试，例如下拉菜单失效、动画失败、条件渲染、
交互状态问题。

对于纯 CSS 修复：完全跳过。CSS 回归会通过重新运行 /design-review 被发现。

如果修复涉及 JS 行为：遵循与 /qa 第 8e.5 阶段相同的流程（研究现有
测试模式，编写一个编码精确缺陷条件的回归测试，运行它，如果通过则提交，失败则延后处理）。
提交格式：`test(design): regression test for FINDING-NNN`。

### 8f. 自我约束（停止并评估）

每修复 5 个问题（或每次发生回退后），计算设计修复风险等级：

```
DESIGN-FIX RISK:
  起始为 0%
  每次回退：                      +15%
  每个仅 CSS 文件变更：           +0%   （安全，只涉及样式）
  每个 JSX/TSX/组件文件变更：     +5%   每个文件
  修复达到第 10 个后：            +1%   每增加一个修复
  触及无关文件：                  +20%
```

**如果风险 > 20%：** 立即停止。向用户展示你目前为止所做的工作，并询问是否继续。

**硬上限：30 个修复。** 达到 30 个修复后，无论是否还有剩余问题，都必须停止。

---

## 阶段 9：最终设计审查

在所有修复都应用后：

1. 在所有受影响页面上重新运行设计审查
2. 计算最终设计分数和 AI 拼凑感分数
3. **如果最终分数比基线更差：** 明显警告，说明发生了回归

---

## 阶段 10：报告

将报告写入本地和项目作用域位置：

**本地：** `.gstack/design-reports/design-audit-{domain}-{YYYY-MM-DD}.md`

**项目作用域：**
```bash
source <(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null) && mkdir -p ~/.gstack/projects/$SLUG
```
写入 `~/.gstack/projects/{slug}/{user}-{branch}-design-audit-{datetime}.md`

**每个问题的附加项**（除了标准设计审查报告之外）：
- 修复状态：verified / best-effort / reverted / deferred
- Commit SHA（如果已修复）
- 已修改文件（如果已修复）
- 修复前/修复后截图（如果已修复）

**摘要部分：**
- 问题总数
- 已应用的修复（verified: X, best-effort: Y, reverted: Z）
- Deferred 问题
- 设计分数变化：baseline → final
- AI 拼凑感分数变化：baseline → final

**PR 摘要：** 包含一行适用于 PR 描述的摘要：
> "Design review found N issues, fixed M. Design score X → Y, AI slop score X → Y."

---

## 阶段 11：更新 TODOS.md

如果仓库中有 `TODOS.md`：

1. **新的 deferred 设计问题** → 作为 TODO 添加，并附上影响级别、类别和描述
2. **已修复且原本在 TODOS.md 中的问题** → 标注为“由 /design-review 在 {branch}, {date} 修复”

---

## 附加规则（design-review 专用）

11. **必须使用干净的工作树。** 如果不干净，在继续前使用 AskUserQuestion 提供 commit/stash/abort 选项。
12. **每个修复一个提交。** 绝不要将多个设计修复打包到一个提交中。
13. **只在第 8e.5 阶段生成回归测试时修改测试。** 绝不要修改 CI 配置。绝不要修改现有测试，只能创建新的测试文件。
14. **发生回归时回退。** 如果某个修复让情况变得更糟，立即执行 `git revert HEAD`。
15. **自我约束。** 遵循 design-fix risk 启发式规则。如有疑问，停止并询问。
16. **CSS 优先。** 优先进行 CSS/样式修改，而不是结构性组件修改。纯 CSS 修改更安全，也更容易回退。
17. **导出 DESIGN.md。** 如果用户接受第 2 阶段中的提议，你可以写入一个 DESIGN.md 文件。