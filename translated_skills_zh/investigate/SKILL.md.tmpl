---
name: investigate
version: 1.0.0
description: |
  通过根因调查进行系统化调试。分为四个阶段：investigate、
  analyze、hypothesize、implement。铁律：未找到根因之前不得修复。
  当被要求“调试这个”“修复这个 bug”“为什么这个坏了”，
  “调查这个错误”或“做根因分析”时使用。
  当用户报告错误、异常行为，或正在排查某些功能为何停止工作时，
  主动建议使用。
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - AskUserQuestion
  - WebSearch
hooks:
  PreToolUse:
    - matcher: "Edit"
      hooks:
        - type: command
          command: "bash ${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"
          statusMessage: "正在检查调试范围边界..."
    - matcher: "Write"
      hooks:
        - type: command
          command: "bash ${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh"
          statusMessage: "正在检查调试范围边界..."
---

{{PREAMBLE}}

# 系统化调试

## 铁律

**在先做根因调查之前，禁止修复。**

修复表象只会造成打地鼠式调试。每一个没有解决根因的修复，都会让下一个 bug 更难找到。先找到根因，再修复它。

---

## 阶段 1：根因调查

在形成任何假设之前，先收集上下文。

1. **收集症状：** 阅读错误信息、堆栈跟踪和复现步骤。如果用户提供的上下文还不够，每次只通过 AskUserQuestion 提问 ONE 个问题。

2. **阅读代码：** 从症状沿着代码路径回溯到潜在原因。使用 Grep 找出所有引用，使用 Read 理解逻辑。

3. **检查最近的变更：**
   ```bash
   git log --oneline -20 -- <affected-files>
   ```
   之前这是正常工作的吗？发生了什么变化？如果是回归问题，根因就在 diff 中。

4. **复现：** 你能稳定触发这个 bug 吗？如果不能，先收集更多证据，再继续。

输出：**"Root cause hypothesis: ..."** —— 针对哪里出了问题以及为什么会出问题，给出一个具体且可验证的判断。

---

## 范围锁定

在形成根因假设之后，将编辑范围锁定到受影响的模块，防止范围蔓延。

```bash
[ -x "${CLAUDE_SKILL_DIR}/../freeze/bin/check-freeze.sh" ] && echo "FREEZE_AVAILABLE" || echo "FREEZE_UNAVAILABLE"
```

**如果是 FREEZE_AVAILABLE：** 找出包含受影响文件的最小目录。把它写入 freeze 状态文件：

```bash
STATE_DIR="${CLAUDE_PLUGIN_DATA:-$HOME/.gstack}"
mkdir -p "$STATE_DIR"
echo "<detected-directory>/" > "$STATE_DIR/freeze-dir.txt"
echo "Debug scope locked to: <detected-directory>/"
```

将 `<detected-directory>` 替换为实际目录路径（例如 `src/auth/`）。告诉用户：“本次调试会话中，编辑已限制在 `<dir>/`。这样可以防止修改无关代码。运行 `/unfreeze` 可移除该限制。”

如果 bug 横跨整个仓库，或范围确实无法明确，就跳过锁定，并说明原因。

**如果是 FREEZE_UNAVAILABLE：** 跳过范围锁定。编辑不受限制。

---

## 阶段 2：模式分析

检查这个 bug 是否符合已知模式：

| 模式 | 特征 | 应查看的位置 |
|---------|-----------|---------------|
| 竞争条件 | 间歇性出现、依赖时序 | 对共享状态的并发访问 |
| Nil/null 传播 | NoMethodError、TypeError | 对可选值缺少保护判断 |
| 状态损坏 | 数据不一致、部分更新 | 事务、回调、hooks |
| 集成失败 | 超时、意外响应 | 外部 API 调用、服务边界 |
| 配置漂移 | 本地正常，在 staging/prod 失败 | Env vars、feature flags、DB 状态 |
| 过期缓存 | 显示旧数据，清理缓存后恢复 | Redis、CDN、浏览器缓存、Turbo |

同时检查：
- `TODOS.md` 中是否有相关的已知问题
- `git log` 中同一区域的历史修复 —— **同一批文件里反复出现的 bug 是架构异味**，不是巧合

**外部模式搜索：** 如果这个 bug 不符合上面的已知模式，使用 WebSearch 搜索：
- `"{framework} {generic error type}"` —— **先脱敏：** 去掉主机名、IP、文件路径、SQL、客户数据。搜索错误类别，不要搜索原始消息。
- `"{library} {component} known issues"`

如果 WebSearch 不可用，就跳过这一步并继续进行假设测试。如果发现有文档记录的解决方案或已知依赖缺陷，在阶段 3 中将其作为候选假设提出。

---

## 阶段 3：假设测试

在编写任何修复之前，先验证你的假设。

1. **确认假设：** 在疑似根因处添加临时日志语句、断言或调试输出。运行复现。证据是否吻合？

2. **如果假设错误：** 在形成下一个假设之前，考虑搜索该错误。**先脱敏** —— 去掉主机名、IP、文件路径、SQL 片段、客户标识符，以及任何内部/专有数据。只搜索通用错误类型和框架上下文：`"{component} {sanitized error type} {framework version}"`。如果错误信息过于具体，无法安全脱敏，就跳过搜索。如果 WebSearch 不可用，也跳过并继续。然后回到阶段 1。收集更多证据。不要猜。

3. **3 次失败规则：** 如果连续 3 个假设都失败，**停止**。使用 AskUserQuestion：
   ```
   已测试 3 个假设，均不匹配。这可能是架构问题，
   而不只是一个简单 bug。

   A) 继续调查 — 我有一个新假设：[描述]
   B) 升级为人工审查 — 这需要熟悉该系统的人处理
   C) 增加日志并等待 — 为该区域加埋点，下次再捕获
   ```

**危险信号** —— 如果你看到以下任一情况，放慢节奏：
- “先快速修一下” —— 不存在“先修一下”。要么正确修复，要么升级处理。
- 在追踪数据流之前就提出修复方案 —— 你是在猜。
- 每次修复都会在别处暴露出新问题 —— 错的是层次，不是代码。

---

## 阶段 4：实施

一旦确认根因：

1. **修复根因，而不是症状。** 做出能够消除实际问题的最小改动。

2. **最小 diff：** 修改尽可能少的文件、尽可能少的行数。克制住顺手重构相邻代码的冲动。

3. **编写一个回归测试**，要求：
   - 在没有修复时会**失败**（证明测试有意义）
   - 在修复后会**通过**（证明修复有效）

4. **运行完整测试套件。** 粘贴输出。不允许引入回归。

5. **如果修复涉及超过 5 个文件：** 使用 AskUserQuestion 提示影响半径：
   ```
   这次修复涉及 N 个文件。对于 bug 修复来说，影响半径较大。
   A) 继续 — 根因确实横跨这些文件
   B) 拆分 — 先修复关键路径，其余部分延后
   C) 重新考虑 — 也许存在更有针对性的方案
   ```

---

## 阶段 5：验证与报告

**重新验证：** 复现最初的 bug 场景，并确认问题已修复。这不是可选项。

运行测试套件并粘贴输出。

输出一份结构化调试报告：
```
DEBUG REPORT
════════════════════════════════════════
Symptom:         [用户观察到的现象]
Root cause:      [实际出错的原因]
Fix:             [做了哪些修改，附 file:line 引用]
Evidence:        [测试输出、证明修复有效的复现结果]
Regression test: [新测试的 file:line]
Related:         [TODOS.md 条目、同一区域的历史 bug、架构说明]
Status:          DONE | DONE_WITH_CONCERNS | BLOCKED
════════════════════════════════════════
```

---

## 重要规则

- **修复尝试失败达到 3 次以上 → 停止，并质疑架构。** 这是架构错误，不是假设失败。
- **绝不要应用无法验证的修复。** 如果你不能复现并确认，就不要交付。
- **绝不要说“这应该能修好”。** 要验证并证明它。运行测试。
- **如果修复涉及 >5 个文件 → 继续之前先用 AskUserQuestion 询问影响半径。**
- **完成状态：**
  - DONE — 已找到根因，已应用修复，已编写回归测试，全部测试通过
  - DONE_WITH_CONCERNS — 已修复，但无法完全验证（例如间歇性 bug、需要 staging）
  - BLOCKED — 调查后根因仍不清楚，已升级处理