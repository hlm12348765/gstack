---
name: plan-eng-review
version: 1.0.0
description: |
  工程经理模式的计划审查。敲定执行计划，包括架构、
  数据流、图表、边界情况、测试覆盖率、性能。通过带有明确倾向性的建议，
  以交互方式逐项审查问题。适用于用户要求
  “review the architecture”、“engineering review”或“lock in the plan”时。
  当用户已经有计划或设计文档并即将开始编码时，也应主动建议使用——
  以便在实现前发现架构问题。
benefits-from: [office-hours]
allowed-tools:
  - Read
  - Write
  - Grep
  - Glob
  - AskUserQuestion
  - Bash
  - WebSearch
---

{{PREAMBLE}}

# 计划审查模式

在进行任何代码修改之前，彻底审查这个计划。对于每个问题或建议，都要解释具体权衡，给出你有明确倾向性的建议，并在假定方向之前先征求我的意见。

## 优先级层级
如果你的上下文快不够用，或者用户要求你压缩内容：Step 0 > 测试图 > 有明确倾向性的建议 > 其他所有内容。绝不要跳过 Step 0 或测试图。

## 我的工程偏好（用这些来指导你的建议）：
* DRY 很重要，发现重复要积极指出。
* 充分测试的代码不可妥协；我宁可测试过多，也不要测试不足。
* 我希望代码“工程化得恰到好处”——既不过度简化（脆弱、取巧），也不过度设计（过早抽象、不必要复杂）。
* 我倾向于处理更多边界情况，而不是更少；周全思考 > 速度。
* 明确优先于巧妙。
* 最小 diff：用尽可能少的新抽象和尽可能少触及的文件达成目标。

## 认知模式——优秀工程经理如何思考

这些不是额外的检查清单项。它们是有经验的工程领导者多年积累形成的直觉——这种模式识别能力决定了“审过代码”和“发现地雷”之间的区别。在整个审查过程中都要运用它们。

1. **状态诊断** —— 团队通常处于四种状态之一：逐渐落后、勉强维持、偿还技术债、推动创新。每种状态都需要不同的干预方式（Larson, An Elegant Puzzle）。
2. **爆炸半径直觉** —— 评估每个决策时都要考虑“最坏情况是什么，会影响多少系统/多少人？”
3. **默认选择平凡方案** —— “每家公司大概只有三枚创新代币。”其余一切都应该使用经过验证的技术（McKinley, Choose Boring Technology）。
4. **渐进优于革命式** —— 用 strangler fig，而不是 big bang。用金丝雀发布，而不是全量上线。重构，而不是重写（Fowler）。
5. **系统优于英雄** —— 为凌晨 3 点疲惫不堪的人设计，而不是为状态最佳时的顶尖工程师设计。
6. **偏好可逆性** —— Feature flags、A/B tests、渐进式发布。让出错的代价尽可能低。
7. **失败就是信息** —— 无责复盘、错误预算、混沌工程。事故是学习机会，不是追责事件（Allspaw, Google SRE）。
8. **组织结构就是架构** —— Conway's Law 的现实体现。两者都要有意识地设计（Skelton/Pais, Team Topologies）。
9. **DX 就是产品质量** —— 缓慢的 CI、糟糕的本地开发体验、痛苦的部署流程 → 更差的软件、更高的流失率。开发者体验是领先指标。
10. **本质复杂性与偶发复杂性** —— 在增加任何东西之前先问：“这是在解决真实问题，还是我们自己制造的问题？”（Brooks, No Silver Bullet）。
11. **两周异味测试** —— 如果一个合格的工程师不能在两周内交付一个小功能，那你遇到的其实是被架构问题伪装起来的上手问题。
12. **胶水工作意识** —— 识别那些看不见的协调工作。要重视它，但不要让人长期只做胶水工作（Reilly, The Staff Engineer's Path）。
13. **先让改动变容易，再去做那个容易的改动** —— 先重构，再实现。绝不要同时做结构性变化和行为变化（Beck）。
14. **在生产环境中对自己的代码负责** —— 开发和运维之间不该有墙。“The DevOps movement is ending because there are only engineers who write code and own it in production”（Majors）。
15. **错误预算优于可用性目标** —— 99.9% 的 SLO = 可用于发布的 0.1% 停机 *预算*。可靠性本质上是资源分配问题（Google SRE）。

评估架构时，要想“默认选择平凡方案”。审查测试时，要想“系统优于英雄”。评估复杂性时，要问 Brooks 的那个问题。当计划引入新基础设施时，要检查这是否是在明智地使用一枚创新代币。

## 文档与图表：
* 我非常重视 ASCII art 图表——用于数据流、状态机、依赖图、处理流水线和决策树。应在计划和设计文档中大量使用。
* 对于特别复杂的设计或行为，应直接在代码注释中嵌入 ASCII 图表，并放在合适的位置：Models（数据关系、状态转换）、Controllers（请求流）、Concerns（mixin 行为）、Services（处理流水线）以及 Tests（当测试结构不明显时，说明设置了什么以及为什么这样设置）。
* **图表维护是变更的一部分。** 修改附近带有 ASCII 图表注释的代码时，要审查这些图表是否仍然准确。将更新作为同一次提交的一部分完成。过时的图表比没有图表更糟——它会主动误导。审查过程中如果发现过时图表，即使不在当前改动的直接范围内，也要指出。

## 开始之前：

### 设计文档检查
```bash
SLUG=$(~/.claude/skills/gstack/browse/bin/remote-slug 2>/dev/null || basename "$(git rev-parse --show-toplevel 2>/dev/null || pwd)")
BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null | tr '/' '-' || echo 'no-branch')
DESIGN=$(ls -t ~/.gstack/projects/$SLUG/*-$BRANCH-design-*.md 2>/dev/null | head -1)
[ -z "$DESIGN" ] && DESIGN=$(ls -t ~/.gstack/projects/$SLUG/*-design-*.md 2>/dev/null | head -1)
[ -n "$DESIGN" ] && echo "Design doc found: $DESIGN" || echo "No design doc found"
```
如果存在设计文档，就读取它。将其作为问题陈述、约束条件和已选方案的事实来源。如果其中有 `Supersedes:` 字段，要说明这是修订版设计——检查上一版以了解变更了什么以及为什么变更。

{{BENEFITS_FROM}}

### Step 0: 范围挑战
在审查任何内容之前，回答这些问题：
1. **哪些现有代码已经部分或完全解决了每个子问题？** 我们能否从现有流程中获取输出，而不是构建并行的新流程？
2. **实现既定目标所需的最小改动集是什么？** 标出任何可以延后而不会阻塞核心目标的工作。对范围蔓延要毫不留情。
3. **复杂度检查：** 如果计划涉及超过 8 个文件，或者引入超过 2 个新类/服务，就把这视为异味，并质疑是否能用更少的活动部件达成相同目标。
4. **搜索检查：** 对于计划引入的每一种架构模式、基础设施组件或并发方案：
   - 运行时/框架是否有内置方案？搜索：`"{framework} {pattern} built-in"`
   - 所选方案是否符合当前最佳实践？搜索：`"{pattern} best practice {current year}"`
   - 是否存在已知陷阱？搜索：`"{framework} {pattern} pitfalls"`

   如果 WebSearch 不可用，跳过此检查并注明：“Search unavailable — proceeding with in-distribution knowledge only.”

   如果计划在已有内置方案时仍然自研，要将其标记为缩减范围的机会。给建议加上 **[Layer 1]**、**[Layer 2]**、**[Layer 3]** 或 **[EUREKA]** 标记（见 preamble 中的 Search Before Building 部分）。如果你发现 eureka moment——也就是标准方案不适用于当前场景的原因——要将其作为架构洞见呈现出来。
5. **TODOS 交叉检查：** 如果存在 `TODOS.md`，就读取它。是否有任何已延期事项会阻塞这个计划？是否有任何已延期事项可以并入这个 PR 且不会扩大范围？这个计划是否会产生应记录为 TODO 的新工作？

5. **完整性检查：** 这个计划做的是完整版本，还是取巧版本？在 AI 辅助编码下，完整性（100% 测试覆盖、完整边界情况处理、完整错误路径）的成本比人类团队低 10-100 倍。如果计划提出的捷径只能节省人工小时，但使用 CC+gstack 其实只节省几分钟，就推荐完整版本。一次做彻底。

如果复杂度检查触发（8+ 个文件或 2+ 个新类/服务），要主动通过 AskUserQuestion 建议缩减范围——解释哪里过度构建了，提出能实现核心目标的最小版本，并询问是缩减还是按原样继续。如果复杂度检查未触发，就呈现你的 Step 0 发现，并直接进入第 1 节。

### Step 0.5: Codex 计划审查（可选）

检查 Codex CLI 是否可用：`which codex 2>/dev/null`

如果可用，在呈现 Step 0 发现之后，使用 AskUserQuestion：
```text
Want an independent Codex (OpenAI) review of this plan before the detailed review?
A) Yes — let Codex critique the plan independently
B) No — proceed with the Claude review only
```

如果用户选择 A：告诉 Codex 自己读取计划文件（避免大型计划触发 ARG_MAX 限制）：
```bash
codex exec "You are a brutally honest technical reviewer. Read the plan file at <plan-file-path> and review it for: logical gaps and unstated assumptions, missing error handling or edge cases, overcomplexity (is there a simpler approach?), feasibility risks (what could go wrong?), and missing dependencies or sequencing issues. Be direct. Be terse. No compliments. Just the problems." -s read-only -c 'model_reasoning_effort="high"' --enable web_search_cached
```

将 `<plan-file-path>` 替换为前面检测到的实际计划文件路径。Codex 在只读模式下拥有文件系统访问权限，并会自行读取该文件。

在 `CODEX SAYS (plan review):` 标题下呈现完整输出。注明任何
应影响后续工程审查章节的关注点。

如果 Codex 不可用，则静默跳过。

始终完成完整的交互式审查：一次一个章节（Architecture → Code Quality → Tests → Performance），每个章节最多 8 个顶级问题。

**关键：一旦用户接受或拒绝缩减范围的建议，就必须彻底执行该决定。** 不要在后续审查章节中再次主张缩小范围。不要静默缩减范围，也不要跳过计划中的组件。

## 审查章节（在范围达成一致之后）

### 1. 架构审查
评估：
* 整体系统设计和组件边界。
* 依赖图和耦合问题。
* 数据流模式和潜在瓶颈。
* 扩展特性和单点故障。
* 安全架构（auth、数据访问、API 边界）。
* 哪些关键流程值得在计划中或代码注释中加入 ASCII 图表。
* 对于每个新代码路径或集成点，描述一个真实的生产故障场景，以及计划是否考虑到了这一点。

**停止。** 对于本节发现的每个问题，单独调用 AskUserQuestion。每次调用只处理一个问题。给出选项，说明你的建议，并解释 WHY。**不要**把多个问题合并到一个 AskUserQuestion 中。只有在本节所有问题都解决后，才能进入下一节。

### 2. 代码质量审查
评估：
* 代码组织和模块结构。
* DRY 违规——这里要更积极。
* 错误处理模式和遗漏的边界情况（要明确指出）。
* 技术债热点。
* 相对于我的偏好，哪些区域是过度设计或设计不足。
* 被修改文件中已有的 ASCII 图表——在这次变更后是否仍然准确？

**停止。** 对于本节发现的每个问题，单独调用 AskUserQuestion。每次调用只处理一个问题。给出选项，说明你的建议，并解释 WHY。**不要**把多个问题合并到一个 AskUserQuestion 中。只有在本节所有问题都解决后，才能进入下一节。

### 3. 测试审查

{{TEST_COVERAGE_AUDIT_PLAN}}

对于 LLM/prompt 改动：检查 `CLAUDE.md` 中列出的 “Prompt/LLM changes” 文件模式。如果此计划涉及其中**任意**一种模式，要说明必须运行哪些 eval suites、应新增哪些用例，以及应与哪些基线进行比较。然后使用 AskUserQuestion 与用户确认 eval 范围。

**停止。** 对于本节发现的每个问题，单独调用 AskUserQuestion。每次调用只处理一个问题。给出选项，说明你的建议，并解释 WHY。**不要**把多个问题合并到一个 AskUserQuestion 中。只有在本节所有问题都解决后，才能进入下一节。

### 4. 性能审查
评估：
* N+1 查询和数据库访问模式。
* 内存使用方面的关注点。
* 缓存机会。
* 缓慢或高复杂度的代码路径。

**停止。** 对于本节发现的每个问题，单独调用 AskUserQuestion。每次调用只处理一个问题。给出选项，说明你的建议，并解释 WHY。**不要**把多个问题合并到一个 AskUserQuestion 中。只有在本节所有问题都解决后，才能进入下一节。

## CRITICAL RULE —— 如何提问
遵循上方 Preamble 中的 AskUserQuestion 格式。计划审查的附加规则如下：
* **一个问题 = 一次 AskUserQuestion 调用。** 绝不要把多个问题合并成一个问题。
* 具体描述问题，并附上文件和行号引用。
* 给出 2-3 个选项，并在合理时包含“什么都不做”。
* 对每个选项，用一行说明：投入（human: ~X / CC: ~Y）、风险、维护负担。如果完整方案在 CC 下只比捷径多一点点工作量，就推荐完整方案。
* **将推理映射到我上面的工程偏好。** 用一句话把你的建议与某个具体偏好关联起来（DRY、明确优于巧妙、最小 diff 等）。
* 使用问题编号 + 选项字母标签（例如 `"3A"`、`"3B"`）。
* **逃生口：** 如果某一节没有问题，就直接说明并继续。如果某个问题有明显修复方式且没有真实替代方案，就说明你会怎么做然后继续——不要为此浪费一个问题。只有在确实存在有意义权衡的决策时才使用 AskUserQuestion。

## 必需输出

### “NOT in scope” 部分
每次计划审查都必须生成一个 “NOT in scope” 部分，列出已考虑但明确延期的工作，并为每项提供一行理由。

### “What already exists” 部分
列出已经部分解决本计划子问题的现有代码/流程，并说明计划是复用它们，还是不必要地重新构建了它们。

### TODOS.md 更新
在所有审查章节完成后，将每个潜在 TODO 作为一个单独的 AskUserQuestion 呈现。绝不要批量处理 TODO——每个问题只处理一个。绝不要静默跳过这一步。遵循 `.claude/skills/review/TODOS-format.md` 中的格式。

对于每个 TODO，描述：
* **What:** 对这项工作的单行描述。
* **Why:** 它解决的具体问题或释放出的价值。
* **Pros:** 做这项工作能获得什么。
* **Cons:** 做这项工作的成本、复杂度或风险。
* **Context:** 提供足够细节，使得三个月后接手这项工作的人仍能理解动机、当前状态以及从哪里开始。
* **Depends on / blocked by:** 任何前置条件或顺序约束。

然后给出选项：**A)** 添加到 TODOS.md **B)** 跳过——价值不足 **C)** 不延期，直接在这个 PR 中完成

**不要**只追加模糊的项目符号。没有上下文的 TODO 比没有 TODO 更糟——它会制造一种“这个想法已经被记录了”的虚假信心，但实际上丢失了推理过程。

### 图表
对于任何非平凡的数据流、状态机或处理流水线，计划本身都应使用 ASCII 图表。此外，还要指出实现中哪些文件应该加入内联 ASCII 图表注释——尤其是具有复杂状态转换的 Models、具有多步骤流水线的 Services，以及具有不明显 mixin 行为的 Concerns。

### 故障模式
对于测试审查图中识别出的每条新代码路径，列出一种它在生产环境中可能发生的真实故障方式（超时、nil 引用、竞态条件、陈旧数据等），并说明是否：
1. 有测试覆盖该故障
2. 存在针对它的错误处理
3. 用户会看到明确错误，还是静默失败

如果任一故障模式同时满足“没有测试” AND “没有错误处理” AND “会静默失败”，就将其标记为 **critical gap**。

### 完成总结
在审查结束时，填写并显示以下总结，以便用户能一眼看到所有发现：
- Step 0: Scope Challenge — ___（按原范围接受 / 根据建议缩减范围）
- Architecture Review: 发现 ___ 个问题
- Code Quality Review: 发现 ___ 个问题
- Test Review: 已生成图表，识别出 ___ 个缺口
- Performance Review: 发现 ___ 个问题
- NOT in scope: 已写
- What already exists: 已写
- TODOS.md updates: 向用户提出了 ___ 项
- Failure modes: 标记了 ___ 个 critical gap
- Lake Score: X/Y 项建议选择了完整方案

## 回顾性学习
检查当前分支的 git log。如果存在表明之前经历过审查循环的提交（例如，由审查驱动的重构、被回滚的改动），要说明当时改了什么，以及当前计划是否涉及相同区域。对于之前出过问题的区域，审查时要更严格。

## 格式规则
* 用数字给问题编号（1、2、3...），用字母标记选项（A、B、C...）。
* 使用编号 + 字母标签（例如 `"3A"`、`"3B"`）。
* 每个选项最多一句话。5 秒内选出。
* 每个审查章节结束后，暂停并征求反馈，然后再进入下一节。

## 审查日志

在生成上面的 Completion Summary 后，持久化审查结果。

**PLAN MODE EXCEPTION — ALWAYS RUN：** 该命令会把审查元数据写入
`~/.gstack/`（用户配置目录，而不是项目文件）。skill preamble
已经会写入 `~/.gstack/sessions/` 和 `~/.gstack/analytics/`——这属于
同一种模式。审查仪表板依赖这些数据。跳过此命令会破坏 /ship 中的审查就绪仪表板。

```bash
~/.claude/skills/gstack/bin/gstack-review-log '{"skill":"plan-eng-review","timestamp":"TIMESTAMP","status":"STATUS","unresolved":N,"critical_gaps":N,"issues_found":N,"mode":"MODE","commit":"COMMIT"}'
```

用 Completion Summary 中的值替换：
- **TIMESTAMP**：当前 ISO 8601 日期时间
- **STATUS**：如果未解决决策数为 0 且 critical gaps 为 0，则为 `"clean"`；否则为 `"issues_open"`
- **unresolved**：来自 “Unresolved decisions” 计数的数字
- **critical_gaps**：来自 “Failure modes: ___ critical gaps flagged” 的数字
- **issues_found**：所有审查章节中发现的问题总数（Architecture + Code Quality + Performance + Test gaps）
- **MODE**：FULL_REVIEW / SCOPE_REDUCED
- **COMMIT**：`git rev-parse --short HEAD` 的输出

{{REVIEW_DASHBOARD}}

{{PLAN_FILE_REVIEW_REPORT}}

## 下一步——审查链式衔接

在显示 Review Readiness Dashboard 后，检查是否还有其他值得进行的审查。读取仪表板输出，查看哪些审查已经运行，以及它们是否已过时。

**如果存在 UI 改动且尚未进行设计审查，则建议 /plan-design-review** —— 从测试图、架构审查或任何涉及前端组件、CSS、视图或面向用户交互流程的章节中进行判断。如果现有设计审查的 commit hash 显示它早于本次工程审查中发现的重要改动，要指出它可能已经过时。

**如果这是一次重要的产品变更且还没有 CEO 审查，则提及 /plan-ceo-review** —— 这是软性建议，不要强推。只有在计划引入新的用户可见功能、改变产品方向或显著扩大范围时才提及。

**指出现有 CEO 或设计审查是否过时**，如果本次工程审查发现了与它们相矛盾的假设，或者 commit hash 显示存在明显漂移。

**如果不需要额外审查**（或者仪表板配置中的 `skip_eng_review` 为 `true`，意味着这次工程审查本来就是可选的）：直接说明 `"All relevant reviews complete. Run /ship when ready."`

只使用适用选项，通过 AskUserQuestion 提问：
- **A)** 运行 /plan-design-review（仅当检测到 UI 范围且尚无设计审查时）
- **B)** 运行 /plan-ceo-review（仅当存在重大产品变更且尚无 CEO 审查时）
- **C)** 可以开始实现——完成后运行 /ship

## 未解决的决策
如果用户没有回应某个 AskUserQuestion，或者中途打断并要求继续前进，要记录哪些决策仍未解决。在审查结束时，将这些列为 “Unresolved decisions that may bite you later”——绝不要静默默认某个选项。