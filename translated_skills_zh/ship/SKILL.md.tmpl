---
name: ship
version: 1.0.0
description: |
  Ship 工作流：检测并合并基础分支，运行测试，审查 diff，提升 VERSION，更新 CHANGELOG，提交，推送，创建 PR。 当用户要求“ship”“deploy”“push to main”“create a PR”或“merge and push”时使用。
  当用户表示代码已准备好或询问如何部署时，主动建议使用。
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - Agent
  - AskUserQuestion
  - WebSearch
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

# Ship：全自动 Ship 工作流

你正在运行 `/ship` 工作流。这是一个**非交互式、全自动**工作流。不要在任何步骤请求确认。用户说了 `/ship`，这意味着直接执行。一路运行到底，并在最后输出 PR URL。

**仅在以下情况停止：**
- 当前位于基础分支（中止）
- 出现无法自动解决的合并冲突（停止并显示冲突）
- 当前分支内测试失败（既有失败会分诊处理，不会自动阻塞）
- 落地前审查发现需要用户判断的 ASK 项
- 需要提升 MINOR 或 MAJOR 版本（询问，见步骤 4）
- Greptile 审查评论需要用户决策（复杂修复、误报）
- 缺少 TODOS.md 且用户想创建一个（询问，见步骤 5.5）
- TODOS.md 组织混乱且用户想重新整理（询问，见步骤 5.5）

**以下情况绝不停止：**
- 存在未提交变更（始终一并包含）
- 版本提升选择（自动选择 MICRO 或 PATCH，见步骤 4）
- CHANGELOG 内容（根据 diff 自动生成）
- commit message 审批（自动提交）
- 多文件变更集（自动拆分为可 bisect 的 commits）
- TODOS.md 已完成项检测（自动标记）
- 可自动修复的审查发现（死代码、N+1、过期注释，自动修复）
- 测试覆盖缺口（自动生成并提交，或在 PR 正文中标注）

---

## 步骤 1：起飞前检查

1. 检查当前分支。如果位于基础分支或仓库默认分支，**中止**：`"You're on the base branch. Ship from a feature branch."`

2. 运行 `git status`（绝不要使用 `-uall`）。未提交变更始终会被包含，无需询问。

3. 运行 `git diff <base>...HEAD --stat` 和 `git log <base>..HEAD --oneline`，以了解本次要发布的内容。

4. 检查审查就绪状态：

{{REVIEW_DASHBOARD}}

如果 Eng Review 不是 “CLEAR”：

1. **检查该分支上是否已有先前的 override：**
   ```bash
   source <(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)
   grep '"skill":"ship-review-override"' ~/.gstack/projects/$SLUG/$BRANCH-reviews.jsonl 2>/dev/null || echo "NO_OVERRIDE"
   ```
   如果存在 override，显示 dashboard，并注明 “Review gate previously accepted — continuing.” 不要再次询问。

2. **如果不存在 override，**使用 AskUserQuestion：
   - 说明 Eng Review 缺失或存在未解决问题
   - `RECOMMENDATION:` 如果变更显然非常小（少于 20 行、修正错字、仅配置变更），选择 C；对于更大的变更，选择 B
   - 选项：A) Ship anyway  B) Abort — run /plan-eng-review first  C) Change is too small to need eng review
   - 如果缺少 CEO Review，将其作为提示信息说明（“CEO Review not run — recommended for product changes”），但**不要**阻塞
   - 对于 Design Review：运行 `source <(~/.claude/skills/gstack/bin/gstack-diff-scope <base> 2>/dev/null)`。如果 `SCOPE_FRONTEND=true` 且 dashboard 中不存在 design review（plan-design-review 或 design-review-lite），说明：`"Design Review not run — this PR changes frontend code. The lite design check will run automatically in Step 3.5, but consider running /design-review for a full visual audit post-implementation."` 仍然绝不阻塞。

3. **如果用户选择 A 或 C，**持久化该决定，以便后续在此分支上运行 `/ship` 时跳过此 gate：
   ```bash
   source <(~/.claude/skills/gstack/bin/gstack-slug 2>/dev/null)
   echo '{"skill":"ship-review-override","timestamp":"'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'","decision":"USER_CHOICE"}' >> ~/.gstack/projects/$SLUG/$BRANCH-reviews.jsonl
   ```
   将 USER_CHOICE 替换为 `"ship_anyway"` 或 `"not_relevant"`。

---

## 步骤 2：合并基础分支（测试之前）

将基础分支抓取并合并到功能分支，以便测试针对合并后的状态运行：

```bash
git fetch origin <base> && git merge origin/<base> --no-edit
```

**如果存在合并冲突：**尝试自动解决简单冲突（VERSION、schema.rb、CHANGELOG 顺序）。如果冲突复杂或存在歧义，**停止**并显示冲突。

**如果已经是最新：**静默继续。

---

## 步骤 2.5：测试框架引导

{{TEST_BOOTSTRAP}}

---

## 步骤 3：运行测试（基于合并后的代码）

**不要运行 `RAILS_ENV=test bin/rails db:migrate`**，因为 `bin/test-lane` 已经会在内部调用
`db:test:prepare`，它会把 schema 加载到正确的 lane 数据库中。
在没有 INSTANCE 的情况下直接运行测试迁移会命中孤立数据库，并破坏 structure.sql。

并行运行两套测试：

```bash
bin/test-lane 2>&1 | tee /tmp/ship_tests.txt &
npm run test 2>&1 | tee /tmp/ship_vitest.txt &
wait
```

两者完成后，读取输出文件并检查通过/失败状态。

**如果有任何测试失败：**不要立刻停止。应用 Test Failure Ownership Triage：

{{TEST_FAILURE_TRIAGE}}

**完成分诊后：**如果仍有当前分支内失败未修复，**停止**。不要继续。如果所有失败都是既有问题且已处理（已修复、已写入 TODO、已分配或已跳过），继续到步骤 3.25。

**如果全部通过：**静默继续，只需简要记录数量。

---

## 步骤 3.25：Eval Suites（条件执行）

当与 prompt 相关的文件发生变更时，evals 为强制项。如果 diff 中没有 prompt 文件，则完全跳过此步骤。

**1. 检查 diff 是否涉及 prompt 相关文件：**

```bash
git diff origin/<base> --name-only
```

匹配以下模式（来自 CLAUDE.md）：
- `app/services/*_prompt_builder.rb`
- `app/services/*_generation_service.rb`, `*_writer_service.rb`, `*_designer_service.rb`
- `app/services/*_evaluator.rb`, `*_scorer.rb`, `*_classifier_service.rb`, `*_analyzer.rb`
- `app/services/concerns/*voice*.rb`, `*writing*.rb`, `*prompt*.rb`, `*token*.rb`
- `app/services/chat_tools/*.rb`, `app/services/x_thread_tools/*.rb`
- `config/system_prompts/*.txt`
- `test/evals/**/*`（eval 基础设施变更会影响所有 suites）

**如果没有匹配：**输出 `"No prompt-related files changed — skipping evals."`，并继续到步骤 3.5。

**2. 识别受影响的 eval suites：**

每个 eval runner（`test/evals/*_eval_runner.rb`）都会声明 `PROMPT_SOURCE_FILES`，列出哪些源文件会影响它。使用 Grep 检查这些文件，找出哪些 suites 与已变更文件匹配：

```bash
grep -l "changed_file_basename" test/evals/*_eval_runner.rb
```

runner 到测试文件的映射：`post_generation_eval_runner.rb` → `post_generation_eval_test.rb`。

**特殊情况：**
- 对 `test/evals/judges/*.rb`、`test/evals/support/*.rb` 或 `test/evals/fixtures/` 的修改会影响所有使用这些 judges/support 文件的 suites。检查 eval 测试文件中的 imports 以确定影响范围。
- 对 `config/system_prompts/*.txt` 的修改：在 eval runners 中 grep prompt 文件名，以找到受影响的 suites。
- 如果不确定哪些 suites 受到影响，就运行所有可能受影响的 suites。多测好过漏掉回归。

**3. 以 `EVAL_JUDGE_TIER=full` 运行受影响的 suites：**

`/ship` 是合并前 gate，因此始终使用 full tier（Sonnet 结构评审 + Opus persona judges）。

```bash
EVAL_JUDGE_TIER=full EVAL_VERBOSE=1 bin/test-lane --eval test/evals/<suite>_eval_test.rb 2>&1 | tee /tmp/ship_evals.txt
```

如果需要运行多个 suites，按顺序依次运行（每个都需要一个 test lane）。如果第一个 suite 失败，立即停止，不要继续消耗剩余 API 成本。

**4. 检查结果：**

- **如果有任何 eval 失败：**显示失败信息、cost dashboard，并**停止**。不要继续。
- **如果全部通过：**记录通过数量和成本。继续到步骤 3.5。

**5. 保存 eval 输出**，在 PR 正文中包含 eval 结果和 cost dashboard（步骤 8）。

**Tier 参考（供上下文使用，`/ship` 始终使用 `full`）：**
| Tier | 何时使用 | 速度（已缓存） | 成本 |
|------|------|----------------|------|
| `fast` (Haiku) | 开发迭代、冒烟测试 | ~5s（快 14 倍） | ~$0.07/run |
| `standard` (Sonnet) | 默认开发，`bin/test-lane --eval` | ~17s（快 4 倍） | ~$0.37/run |
| `full` (Opus persona) | **`/ship` 和合并前** | ~72s（基线） | ~$1.27/run |

---

## 步骤 3.4：测试覆盖审计

{{TEST_COVERAGE_AUDIT_SHIP}}

---

## 步骤 3.5：落地前审查

审查 diff，找出测试无法发现的结构性问题。

1. 读取 `.claude/skills/review/checklist.md`。如果无法读取该文件，**停止**并报告错误。

2. 运行 `git diff origin/<base>` 以获取完整 diff（范围限定为功能变更相对于刚刚抓取的基础分支）。

3. 按两轮应用审查清单：
   - **第 1 轮（CRITICAL）：**SQL 与数据安全、LLM 输出信任边界
   - **第 2 轮（INFORMATIONAL）：**其余所有类别

{{DESIGN_REVIEW_LITE}}

   将任何设计发现与代码审查发现一并纳入。它们遵循下方相同的 Fix-First 流程。

4. **根据 checklist.md 中的 Fix-First Heuristic，将每条发现分类为 AUTO-FIX 或 ASK。**Critical 发现更偏向 ASK；informational 更偏向 AUTO-FIX。

5. **自动修复所有 AUTO-FIX 项。**应用每个修复。每项修复输出一行：
   `[AUTO-FIXED] [file:line] Problem → what you did`

6. **如果仍有 ASK 项，**在**一次** AskUserQuestion 中展示：
   - 列出每项的编号、严重级别、问题、推荐修复
   - 每项选项：A) Fix  B) Skip
   - 总体 `RECOMMENDATION`
   - 如果 ASK 项不超过 3 个，也可以分别使用单独的 AskUserQuestion

7. **完成所有修复后（自动修复 + 用户批准修复）：**
   - 如果应用了**任何**修复：按文件名提交已修复文件（`git add <fixed-files> && git commit -m "fix: pre-landing review fixes"`），然后**停止**并告诉用户重新运行 `/ship` 以再次测试。
   - 如果没有应用修复（所有 ASK 项都被跳过，或未发现问题）：继续到步骤 4。

8. 输出摘要：`Pre-Landing Review: N issues — M auto-fixed, K asked (J fixed, L skipped)`

   如果未发现问题：`Pre-Landing Review: No issues found.`

保存审查输出，它会进入步骤 8 的 PR 正文。

---

## 步骤 3.75：处理 Greptile 审查评论（如果 PR 已存在）

读取 `.claude/skills/review/greptile-triage.md`，并遵循其中的抓取、过滤、分类以及**升级检测**步骤。

**如果 PR 不存在、`gh` 失败、API 返回错误，或者 Greptile 评论数为零：**静默跳过此步骤。继续到步骤 4。

**如果找到了 Greptile 评论：**

在输出中加入 Greptile 摘要：`+ N Greptile comments (X valid, Y fixed, Z FP)`

在回复任何评论之前，运行 greptile-triage.md 中的**升级检测**算法，以确定应使用 Tier 1（友好）还是 Tier 2（坚定）回复模板。

对于每条已分类评论：

**VALID & ACTIONABLE：**使用 AskUserQuestion，并包含：
- 评论内容（`file:line` 或 `[top-level]` + 正文摘要 + permalink URL）
- `RECOMMENDATION: Choose A because [one-line reason]`
- 选项：A) 立即修复，B) 确认后仍然 ship，C) 这是误报
- 如果用户选择 A：应用修复，提交已修复文件（`git add <fixed-files> && git commit -m "fix: address Greptile review — <brief description>"`），使用 greptile-triage.md 中的**Fix reply template**进行回复（包含 inline diff + 说明），并同时保存到项目级与全局 greptile-history（type: fix）。
- 如果用户选择 C：使用 greptile-triage.md 中的**False Positive reply template**进行回复（包含证据 + 建议的 re-rank），并同时保存到项目级与全局 greptile-history（type: fp）。

**VALID BUT ALREADY FIXED：**使用 greptile-triage.md 中的**Already Fixed reply template**回复，无需 AskUserQuestion：
- 包含已完成内容和修复该问题的 commit SHA
- 同时保存到项目级与全局 greptile-history（type: already-fixed）

**FALSE POSITIVE：**使用 AskUserQuestion：
- 展示评论以及你认为它错误的原因（`file:line` 或 `[top-level]` + 正文摘要 + permalink URL）
- 选项：
  - A) 回复 Greptile，解释这是误报（如果明显错误，推荐）
  - B) 还是修复（如果很简单）
  - C) 静默忽略
- 如果用户选择 A：使用 greptile-triage.md 中的**False Positive reply template**回复（包含证据 + 建议的 re-rank），并同时保存到项目级与全局 greptile-history（type: fp）

**SUPPRESSED：**静默跳过，这些是先前分诊中已知的误报。

**所有评论处理完成后：**如果应用了任何修复，那么步骤 3 的测试结果已经过期。继续到步骤 4 之前，**重新运行测试**（步骤 3）。如果未应用任何修复，则继续到步骤 4。

---

{{ADVERSARIAL_STEP}}

## 步骤 4：版本提升（自动决定）

1. 读取当前 `VERSION` 文件（4 位格式：`MAJOR.MINOR.PATCH.MICRO`）

2. **根据 diff 自动决定提升级别：**
   - 统计变更行数（`git diff origin/<base>...HEAD --stat | tail -1`）
   - **MICRO**（第 4 位）：变更少于 50 行、微小调整、错字、配置
   - **PATCH**（第 3 位）：变更 50 行以上、缺陷修复、小到中型功能
   - **MINOR**（第 2 位）：**询问用户**，仅用于重大功能或显著架构变更
   - **MAJOR**（第 1 位）：**询问用户**，仅用于里程碑或破坏性变更

3. 计算新版本：
   - 提升某一位时，其右侧所有位都重置为 0
   - 示例：`0.19.1.0` + PATCH → `0.19.2.0`

4. 将新版本写入 `VERSION` 文件。

---

## 步骤 5：CHANGELOG（自动生成）

1. 读取 `CHANGELOG.md` 头部以了解格式。

2. 根据**该分支上的全部 commits**自动生成条目（不仅仅是最近的）：
   - 使用 `git log <base>..HEAD --oneline` 查看所有将被 ship 的 commits
   - 使用 `git diff <base>...HEAD` 查看相对于基础分支的完整 diff
   - CHANGELOG 条目必须完整涵盖进入该 PR 的**所有**变更
   - 如果该分支上已有 CHANGELOG 条目覆盖了部分 commits，则用一个针对新版本的统一条目替换它们
   - 将变更归类到适用章节：
     - `### Added`：新功能
     - `### Changed`：现有功能变更
     - `### Fixed`：缺陷修复
     - `### Removed`：移除的功能
   - 编写简洁、描述明确的 bullet points
   - 插入到文件头之后（第 5 行），日期为今天
   - 格式：`## [X.Y.Z.W] - YYYY-MM-DD`

**不要要求用户描述变更。**应从 diff 和 commit 历史中推断。

---

## 步骤 5.5：TODOS.md（自动更新）

将项目的 TODOS.md 与即将 ship 的变更进行交叉核对。自动标记已完成项；只有在文件缺失或组织混乱时才提示用户。

读取 `.claude/skills/review/TODOS-format.md` 作为规范格式参考。

**1. 检查仓库根目录中是否存在 TODOS.md。**

**如果 TODOS.md 不存在：**使用 AskUserQuestion：
- 消息：`"GStack recommends maintaining a TODOS.md organized by skill/component, then priority (P0 at top through P4, then Completed at bottom). See TODOS-format.md for the full format. Would you like to create one?"`
- 选项：A) 现在创建，B) 暂时跳过
- 如果选择 A：创建 `TODOS.md` 骨架（`# TODOS` 标题 + `## Completed` 章节）。继续到步骤 3。
- 如果选择 B：跳过步骤 5.5 的其余部分。继续到步骤 6。

**2. 检查结构与组织：**

读取 TODOS.md，并验证其是否遵循推荐结构：
- 条目按 `## <Skill/Component>` 标题分组
- 每个条目都有 `**Priority:**` 字段，值为 P0-P4
- 底部有一个 `## Completed` 章节

**如果组织混乱**（缺少 priority 字段、没有 component 分组、没有 Completed 章节）：使用 AskUserQuestion：
- 消息：`"TODOS.md doesn't follow the recommended structure (skill/component groupings, P0-P4 priority, Completed section). Would you like to reorganize it?"`
- 选项：A) 现在重新整理（推荐），B) 保持原样
- 如果选择 A：按照 TODOS-format.md 就地重组。保留所有内容，只调整结构，绝不删除条目。
- 如果选择 B：不重组，直接继续到步骤 3。

**3. 检测已完成的 TODO：**

此步骤完全自动化，无需用户交互。

使用前面步骤中已收集的 diff 和 commit 历史：
- `git diff <base>...HEAD`（相对于基础分支的完整 diff）
- `git log <base>..HEAD --oneline`（所有即将 ship 的 commits）

对于每个 TODO 条目，通过以下方式检查此 PR 是否已将其完成：
- 将 commit messages 与 TODO 标题和描述进行匹配
- 检查 TODO 中引用的文件是否出现在 diff 中
- 检查 TODO 所描述的工作是否与功能变更相符

**应保守处理：**只有在 diff 中有明确证据时，才将 TODO 标记为已完成。如果不能确定，就保持不动。

**4. 将已完成项移动**到底部的 `## Completed` 章节。附加：`**Completed:** vX.Y.Z (YYYY-MM-DD)`

**5. 输出摘要：**
- `TODOS.md: N items marked complete (item1, item2, ...). M items remaining.`
- 或：`TODOS.md: No completed items detected. M items remaining.`
- 或：`TODOS.md: Created.` / `TODOS.md: Reorganized.`

**6. 防御性处理：**如果无法写入 TODOS.md（权限错误、磁盘已满），警告用户后继续。绝不要因为 TODOS 失败而中止 ship 工作流。

保存此摘要，它会进入步骤 8 的 PR 正文。

---

## 步骤 6：提交（可 bisect 的块）

**目标：**创建小而逻辑清晰的 commits，使其适合 `git bisect`，并帮助 LLM 理解变更内容。

1. 分析 diff，并将变更分组为逻辑 commits。每个 commit 应代表**一个连贯的变更**，不是一个文件，而是一个逻辑单元。

2. **提交顺序**（较早的 commit 在前）：
   - **基础设施：**migrations、配置变更、路由新增
   - **Models & services：**新 models、services、concerns（连同其 tests）
   - **Controllers & views：**controllers、views、JS/React components（连同其 tests）
   - **VERSION + CHANGELOG + TODOS.md：**始终放在最后一个 commit

3. **拆分规则：**
   - model 与其测试文件放在同一个 commit 中
   - service 与其测试文件放在同一个 commit 中
   - controller、其 views 和其测试放在同一个 commit 中
   - migrations 单独作为一个 commit（或与其支撑的 model 分组）
   - 配置/路由变更可以与其启用的功能分组
   - 如果总 diff 很小（少于 50 行且涉及文件少于 4 个），单个 commit 即可

4. **每个 commit 都必须独立有效**，不能有损坏的 imports，也不能引用尚不存在的代码。按依赖顺序排列 commits，让依赖项先出现。

5. 为每个 commit 组织 commit message：
   - 第一行：`<type>: <summary>`（type = feat/fix/chore/refactor/docs）
   - 正文：简要描述此 commit 包含的内容
   - 只有**最后一个 commit**（VERSION + CHANGELOG）才带版本标签和 co-author trailer：

```bash
git commit -m "$(cat <<'EOF'
chore: bump version and changelog (vX.Y.Z.W)

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## 步骤 6.5：验证 Gate

**铁律：没有最新验证证据，就不得宣称完成。**

在推送之前，如果步骤 4-6 期间代码发生变化，则重新验证：

1. **测试验证：**如果在步骤 3 的测试运行之后有**任何**代码发生变化（来自审查发现的修复，CHANGELOG 编辑不算），重新运行测试套件。粘贴最新输出。步骤 3 的旧输出**不可接受**。

2. **构建验证：**如果项目存在构建步骤，就运行它。粘贴输出。

3. **防止合理化：**
   - `"Should work now"` → 运行它。
   - `"I'm confident"` → 信心不是证据。
   - `"I already tested earlier"` → 代码之后已经变了。再测一次。
   - `"It's a trivial change"` → 微小变更也会破坏生产环境。

**如果这里测试失败：**停止。不要推送。修复问题并回到步骤 3。

没有验证就宣称工作完成，不是高效，而是不诚实。

---

## 步骤 7：推送

推送到远端并设置 upstream tracking：

```bash
git push -u origin <branch-name>
```

---

## 步骤 8：创建 PR

使用 `gh` 创建 pull request：

```bash
gh pr create --base <base> --title "<type>: <summary>" --body "$(cat <<'EOF'
## Summary
<来自 CHANGELOG 的 bullet points>

## Test Coverage
<步骤 3.4 中的 coverage diagram，或 "All new code paths have test coverage.">
<如果步骤 3.4 已运行： "Tests: {before} → {after} (+{delta} new)">

## Pre-Landing Review
<来自步骤 3.5 代码审查的发现，或 "No issues found.">

## Design Review
<如果 design review 已运行： "Design Review (lite): N findings — M auto-fixed, K skipped. AI Slop: clean/N issues.">
<如果没有 frontend 文件变更： "No frontend files changed — design review skipped.">

## Eval Results
<如果已运行 evals：suite 名称、通过/失败数量、cost dashboard 摘要。如果跳过： "No prompt-related files changed — evals skipped.">

## Greptile Review
<如果发现了 Greptile comments：使用 bullet list，带 [FIXED] / [FALSE POSITIVE] / [ALREADY FIXED] 标签，并为每条 comment 提供一行摘要>
<如果未发现 Greptile comments： "No Greptile comments.">
<如果步骤 3.75 期间不存在 PR：完全省略本节>

## TODOS
<如果有条目标记为已完成：使用 bullet list 列出已完成条目及版本>
<如果没有完成条目： "No TODO items completed in this PR.">
<如果创建或重组了 TODOS.md：在此说明>
<如果不存在 TODOS.md 且用户选择跳过：省略本节>

## Test plan
- [x] All Rails tests pass (N runs, 0 failures)
- [x] All Vitest tests pass (N tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

**输出 PR URL**，然后继续到步骤 8.5。

---

## 步骤 8.5：自动调用 /document-release

创建 PR 后，自动同步项目文档。读取
`document-release/SKILL.md` 技能文件（位于该技能目录的相邻位置），并
执行其完整工作流：

1. 读取 `/document-release` 技能：`cat ${CLAUDE_SKILL_DIR}/../document-release/SKILL.md`
2. 按照其说明执行。它会读取项目中的所有 .md 文件，交叉核对
   diff，并更新任何已漂移的内容（README、ARCHITECTURE、CONTRIBUTING、
   CLAUDE.md、TODOS 等）
3. 如果有任何文档被更新，提交变更并推送到同一分支：
   ```bash
   git add -A && git commit -m "docs: sync documentation with shipped changes" && git push
   ```
4. 如果没有文档需要更新，说明：`"Documentation is current — no updates needed."`

此步骤为自动执行。不要请求用户确认。目标是实现零摩擦的
文档更新。用户运行 `/ship` 后，无需额外命令，文档就能保持最新。

---

## 重要规则

- **绝不要跳过测试。**如果测试失败，立即停止。
- **绝不要跳过落地前审查。**如果 checklist.md 无法读取，立即停止。
- **绝不要 force push。**只能使用常规 `git push`。
- **绝不要为了琐碎确认而打断**（例如“ready to push?”、“create PR?”）。以下情况应停止：版本提升（MINOR/MAJOR）、落地前审查发现（ASK 项）、以及 Codex structured review [P1] 发现（仅限大型 diff）。
- **始终使用 VERSION 文件中的 4 位版本格式。**
- **CHANGELOG 中的日期格式：**`YYYY-MM-DD`
- **为可 bisect 性拆分 commits**，每个 commit = 一个逻辑变更。
- **TODOS.md 完成项检测必须保守。**只有当 diff 明确表明工作已完成时，才将条目标记为已完成。
- **使用 greptile-triage.md 中的 Greptile 回复模板。**每条回复都要包含证据（inline diff、代码引用、re-rank 建议）。绝不要发布模糊回复。
- **没有最新验证证据，绝不要推送。**如果步骤 3 的测试之后代码发生变化，推送前必须重新运行。
- **步骤 3.4 会生成 coverage tests。**它们在提交前必须通过。绝不要提交失败的测试。
- **目标是：用户说 `/ship`，接下来看到的就是审查结果 + PR URL + 自动同步完成的文档。**